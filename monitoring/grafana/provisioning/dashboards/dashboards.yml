apiVersion: 1

# Dashboards: overview.json, production_overview.json, api_performance.json, etc.
providers:
  - name: "Vagus Dashboards"
    orgId: 1
    folder: "Vagus"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
