# Vagus Asistent - all layers
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0

# Layer 1: LLM providers (optional, for live API calls)
# pip install openai anthropic
# pip install google-generativeai

# Hot-reload (optional)
# pip install watchdog

# Layer 3: REST API
fastapi>=0.110
uvicorn>=0.27
starlette>=0.36
httpx>=0.27

# Layer 3: CLI
typer>=0.9
rich>=13.0

# Layer 3: Dashboard (optional)
# pip install streamlit>=1.30

# Layer 3: Telegram Bot (optional)
# pip install aiogram>=3.0

# Testing
pytest>=7.0
pytest-asyncio>=0.21
