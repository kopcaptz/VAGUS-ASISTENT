# Vagus Asistent — Core
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0

# Layer 3: REST API / WebSocket / CLI
fastapi>=0.110
uvicorn[standard]>=0.27
PyJWT>=2.8
httpx>=0.27

# LLM Providers (optional — install for live LLM calls)
# pip install openai anthropic google-generativeai

# Hot-reload configuration (optional)
# pip install watchdog

# Database (optional — for production persistence)
# pip install sqlalchemy[asyncio] asyncpg redis

# Streamlit Dashboard (optional)
# pip install streamlit

# Telegram Bot (optional)
# pip install python-telegram-bot

# Testing
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
