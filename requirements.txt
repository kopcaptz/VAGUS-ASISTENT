# Vagus Asistent - Core
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0

# Layer 3: REST API (FastAPI)
fastapi>=0.109
uvicorn[standard]>=0.27
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.6

# Layer 3: CLI
typer>=0.9
rich>=13.0

# Layer 3: HTTP client (CLI, Dashboard, Channel Gateway)
httpx>=0.26

# Layer 3: Telegram bot
aiogram>=3.0

# Layer 3: Web Dashboard
streamlit>=1.30

# Testing
pytest>=7.0
pytest-asyncio>=0.21

# LLM providers (optional)
# pip install openai anthropic google-generativeai

# Hot-reload (optional)
# pip install watchdog

# Layer 2 Semantic Memory (optional)
# pip install chromadb sentence-transformers
